      <p>
    <%= link_to '新しい順', books_path(latest: "true") %>
    |<%= link_to '古い順', books_path(old: "true") %>
    |<%= link_to '評価の高い順', books_path(score_count: "true") %>
      </p>



<table class='table table-hover table-inverse'>
  <thead>
    <tr>
      <th></th>
      <th>Title</th>
      <th>Opinion</th>
      <th colspan="3"></th>
    </tr>
  </thead>
  <tbody>
    <% books.each do |book| %>
      <tr>
        <td><%= link_to(book.user) do %>
          <%= image_tag book.user.get_profile_image, size:'50x50' %>
          <% end %>
        </td>
        <td><%= link_to book.title,book, class: "book_#{book.id}" %></td>
        <td><%= book.body %></td>
        <td class=<%= "favorite-" + book.id.to_s %> >
            <%= render "favorites/btn", book: book %>
        </td>
        <td><%= render "book_comments/counter", book: book %></td>


    <!------------星------------>
        <td>
          <div id="index_raty"></div>
          <script>
            // scriptファイルをどのidの部分に配置するかを指定
            const elemn = document.querySelector('#index_raty');
            const opti = {
              // size     : 9,
              starOff:  '<%= asset_path('star-off.png') %>',
              starOn : '<%= asset_path('star-on.png') %>',
              // starHalf: '<%= asset_path('star-half.png') %>',
              // half: true,

              readOnly: true,
              score: <%= book.score %>,
             };
            raty(elemn,opti);
          </script>
        </td>
    <!----------ここまで---------->


      </tr>
    <% end %>
  </tbody>
</table>